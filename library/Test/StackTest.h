/*********************************************************************
|																																			|
|																																			|
|				In this tests broken stack will be put into functions					|
|																																			|
|																																			|
/*********************************************************************/

#include <stdio.h>
#include "./../source/StackStruct.h"
#include "./../source/StackFunc.h"
#include "cText.h"

//==========================================================================

/**
	*	@brief This functoin test StackDump and print ERROR, if found mistake, OK, if didn't find.
	*/

void T_StackDump();

/**
	*	@brief This functoin test StackDump and print ERROR, if found mistake, OK, if didn't find.
	*/

void T_StackOK();

/**
	*	@brief This functoin test StackDump and print ERROR, if found mistake, OK, if didn't find.
	*/

void T_StackConstruct();

/**
	*	@brief This functoin test StackDump and print ERROR, if found mistake, OK, if didn't find.
	*/

void T_StackPush();

/**
	*	@brief This functoin test StackDump and print ERROR, if found mistake, OK, if didn't find.
	*/

void T_StackPop();

/**
	*	@brief This functoin test StackDump and print ERROR, if found mistake, OK, if didn't find.
	*/

void T_StackDestruct();